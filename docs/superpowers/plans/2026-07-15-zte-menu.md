# zte-menu Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Natywna aplikacja macOS w pasku menu, która pokazuje status modemu ZTE U50 5G (bateria + sygnał), widoczna tylko gdy Mac jest w sieci WiFi `ZTE_B4B622`.

**Architecture:** Warstwowa aplikacja SwiftUI: czysty parser danych (`ModemData` + parsowanie), klient HTTP bez logowania (`ModemClient`), monitor WiFi przez CoreWLAN z bramką uprawnień lokalizacji (`WiFiMonitor`), obserwowalny stan spinający całość (`ModemStore`) i widok `MenuBarExtra`. Logika czysta (parsowanie, mapowania, stany) jest testowana jednostkowo; warstwy systemowe (HTTP, CoreWLAN) trzymane cienkie i wstrzykiwane.

**Tech Stack:** Swift 6.3, SwiftUI (`MenuBarExtra`), SwiftPM (executable target + test target), CoreWLAN, CoreLocation, Foundation `URLSession`. Bez projektu Xcode; pakowanie do `.app` skryptem (skill `macos-spm-app-packaging`).

## Global Constraints

- Swift 6.3, target macOS 14+ (`.macOS(.v14)` w Package.swift). Weryfikacja na macOS 26.5.
- Aplikacja typu **LSUIElement** (brak w Docku) — ustawione w `Info.plist` bundla.
- **Bez logowania do modemu** — dane przez `GET http://192.168.0.1/goform/goform_get_cmd_process`.
- SSID docelowe: `ZTE_B4B622`. IP modemu: `192.168.0.1`. Oba w `Config.swift`.
- Ikona paska: SF Symbol `cellularbars` (z `variableValue`), przy braku sygnału `antenna.radiowaves.left.and.right.slash`.
- Odświeżanie co **60 s** + przy starcie + przy zmianie sieci.
- Pola API tolerują puste stringi (`""` → wartość nieznana, pole ukryte).
- Mapowanie `network_type`: `ENDC` → `5G`. Nieznane → surowa wartość.
- Testy: `swift test`. Warstwy systemowe (URLSession, CoreWLAN, CoreLocation) za protokołami, by dało się je podmienić w testach.
- Referencja pól API i fixtures: `docs/superpowers/specs/2026-07-15-modem-api-findings.md`.

---

### Task 1: Szkielet pakietu SwiftPM

**Files:**
- Create: `Package.swift`
- Create: `Sources/ZteMenu/Placeholder.swift`
- Create: `Tests/ZteMenuTests/SmokeTest.swift`
- Create: `.gitignore`

**Interfaces:**
- Consumes: nic.
- Produces: moduł biblioteczny `ZteMenu` (target testowalny) + późniejszy executable. Struktura `Sources/ZteMenu/…` i `Tests/ZteMenuTests/…`.

- [ ] **Step 1: Napisz `.gitignore`**

```
.build/
.DS_Store
*.xcodeproj
DerivedData/
dist/
```

- [ ] **Step 2: Napisz `Package.swift`**

Uwaga: logika trafia do biblioteki `ZteMenu` (testowalnej), a cienki executable `zte-menu` (Task 9) tylko ją uruchamia.

```swift
// swift-tools-version: 6.0
import PackageDescription

let package = Package(
    name: "ZteMenu",
    platforms: [.macOS(.v14)],
    targets: [
        .target(name: "ZteMenu"),
        .testTarget(name: "ZteMenuTests", dependencies: ["ZteMenu"]),
    ]
)
```

- [ ] **Step 3: Napisz placeholder źródła, by target się kompilował**

`Sources/ZteMenu/Placeholder.swift`:

```swift
enum ZteMenuBuildMarker {
    static let ok = true
}
```

- [ ] **Step 4: Napisz smoke test**

`Tests/ZteMenuTests/SmokeTest.swift`:

```swift
import XCTest
@testable import ZteMenu

final class SmokeTest: XCTestCase {
    func testBuildMarker() {
        XCTAssertTrue(ZteMenuBuildMarker.ok)
    }
}
```

- [ ] **Step 5: Zbuduj i uruchom testy — mają przejść**

Run: `cd /Users/radnok/Developer/radnok/zte-menu && swift test`
Expected: PASS (1 test), build bez błędów.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "chore: szkielet pakietu SwiftPM ZteMenu"
```

---

### Task 2: Konfiguracja (`Config`)

**Files:**
- Create: `Sources/ZteMenu/Config.swift`
- Test: `Tests/ZteMenuTests/ConfigTests.swift`

**Interfaces:**
- Consumes: nic.
- Produces: `enum Config { static let modemBaseURL: URL; static let targetSSID: String; static let refreshInterval: TimeInterval }`.

- [ ] **Step 1: Napisz failing test**

`Tests/ZteMenuTests/ConfigTests.swift`:

```swift
import XCTest
@testable import ZteMenu

final class ConfigTests: XCTestCase {
    func testDefaults() {
        XCTAssertEqual(Config.modemBaseURL.absoluteString, "http://192.168.0.1")
        XCTAssertEqual(Config.targetSSID, "ZTE_B4B622")
        XCTAssertEqual(Config.refreshInterval, 60)
    }
}
```

- [ ] **Step 2: Uruchom test — ma nie przejść**

Run: `swift test --filter ConfigTests`
Expected: FAIL — brak typu `Config`.

- [ ] **Step 3: Zaimplementuj `Config`**

`Sources/ZteMenu/Config.swift`:

```swift
import Foundation

enum Config {
    static let modemBaseURL = URL(string: "http://192.168.0.1")!
    static let targetSSID = "ZTE_B4B622"
    static let refreshInterval: TimeInterval = 60
}
```

- [ ] **Step 4: Uruchom test — ma przejść**

Run: `swift test --filter ConfigTests`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: konfiguracja ZteMenu (IP modemu, SSID, interwał)"
```

---

### Task 3: Model danych i parser (`ModemData`)

Czysta logika — bez sieci. Serce aplikacji. Parsuje surowy słownik `[String: String]` z API modemu na typowany model, tolerując puste stringi.

**Files:**
- Create: `Sources/ZteMenu/ModemData.swift`
- Test: `Tests/ZteMenuTests/ModemDataTests.swift`

**Interfaces:**
- Consumes: nic.
- Produces:
  - `struct ModemData: Equatable { let batteryPercent: Int?; let isCharging: Bool; let signalBars: Int; let networkType: String; let provider: String?; let rsrp: Int?; let sinr: Double?; let isOnline: Bool }`
  - `static func ModemData.parse(_ raw: [String: String]) -> ModemData`
  - `var ModemData.networkLabel: String` — mapuje `networkType` (`ENDC`→`5G`).
  - `var ModemData.signalDescription: String` — opis słowny wg `signalBars`.

- [ ] **Step 1: Napisz failing testy**

`Tests/ZteMenuTests/ModemDataTests.swift`:

```swift
import XCTest
@testable import ZteMenu

final class ModemDataTests: XCTestCase {
    // Fixture: realna odpowiedź modemu (patrz modem-api-findings.md)
    private let raw: [String: String] = [
        "network_type": "ENDC",
        "signalbar": "5",
        "battery_value": "60",
        "battery_charging": "0",
        "ppp_status": "ppp_connected",
        "network_provider": "T-Mobile.pl",
        "Z5g_rsrp": "-81",
        "Z5g_SINR": "33.0",
    ]

    func testParsesBattery() {
        let d = ModemData.parse(raw)
        XCTAssertEqual(d.batteryPercent, 60)
        XCTAssertFalse(d.isCharging)
    }

    func testParsesSignal() {
        let d = ModemData.parse(raw)
        XCTAssertEqual(d.signalBars, 5)
        XCTAssertEqual(d.rsrp, -81)
        XCTAssertEqual(d.sinr, 33.0)
    }

    func testParsesNetworkAndProvider() {
        let d = ModemData.parse(raw)
        XCTAssertEqual(d.networkType, "ENDC")
        XCTAssertEqual(d.networkLabel, "5G")
        XCTAssertEqual(d.provider, "T-Mobile.pl")
        XCTAssertTrue(d.isOnline)
    }

    func testEmptyStringsBecomeNil() {
        let d = ModemData.parse([
            "signalbar": "3",
            "battery_value": "",
            "Z5g_rsrp": "",
            "Z5g_SINR": "",
            "network_type": "LTE",
            "ppp_status": "ppp_disconnected",
        ])
        XCTAssertNil(d.batteryPercent)
        XCTAssertNil(d.rsrp)
        XCTAssertNil(d.sinr)
        XCTAssertEqual(d.signalBars, 3)
        XCTAssertFalse(d.isOnline)
    }

    func testChargingFlag() {
        var r = raw; r["battery_charging"] = "1"
        XCTAssertTrue(ModemData.parse(r).isCharging)
    }

    func testUnknownNetworkTypeFallsBackToRaw() {
        var r = raw; r["network_type"] = "WCDMA"
        XCTAssertEqual(ModemData.parse(r).networkLabel, "WCDMA")
    }

    func testSignalDescription() {
        var r = raw; r["signalbar"] = "5"
        XCTAssertEqual(ModemData.parse(r).signalDescription, "Bardzo dobry")
        r["signalbar"] = "0"
        XCTAssertEqual(ModemData.parse(r).signalDescription, "Brak")
    }
}
```

- [ ] **Step 2: Uruchom testy — mają nie przejść**

Run: `swift test --filter ModemDataTests`
Expected: FAIL — brak typu `ModemData`.

- [ ] **Step 3: Zaimplementuj `ModemData`**

`Sources/ZteMenu/ModemData.swift`:

```swift
import Foundation

struct ModemData: Equatable {
    let batteryPercent: Int?
    let isCharging: Bool
    let signalBars: Int
    let networkType: String
    let provider: String?
    let rsrp: Int?
    let sinr: Double?
    let isOnline: Bool

    static func parse(_ raw: [String: String]) -> ModemData {
        func str(_ key: String) -> String? {
            guard let v = raw[key], !v.isEmpty else { return nil }
            return v
        }
        return ModemData(
            batteryPercent: str("battery_value").flatMap { Int($0) },
            isCharging: raw["battery_charging"] == "1",
            signalBars: str("signalbar").flatMap { Int($0) } ?? 0,
            networkType: str("network_type") ?? "",
            provider: str("network_provider"),
            rsrp: str("Z5g_rsrp").flatMap { Int($0) },
            sinr: str("Z5g_SINR").flatMap { Double($0) },
            isOnline: raw["ppp_status"] == "ppp_connected"
        )
    }

    var networkLabel: String {
        switch networkType {
        case "ENDC": return "5G"
        case "": return "—"
        default: return networkType
        }
    }

    var signalDescription: String {
        switch signalBars {
        case ...0: return "Brak"
        case 1: return "Bardzo słaby"
        case 2: return "Słaby"
        case 3: return "Średni"
        case 4: return "Dobry"
        default: return "Bardzo dobry"
        }
    }
}
```

- [ ] **Step 4: Uruchom testy — mają przejść**

Run: `swift test --filter ModemDataTests`
Expected: PASS (7 testów).

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: model i parser danych modemu (ModemData)"
```

---

### Task 4: Klient HTTP modemu (`ModemClient`)

Cienka warstwa nad `URLSession`. Buduje URL zapytania, dekoduje JSON do `[String: String]`, oddaje `ModemData`. Sesja wstrzykiwana przez protokół, by testować bez sieci.

**Files:**
- Create: `Sources/ZteMenu/ModemClient.swift`
- Test: `Tests/ZteMenuTests/ModemClientTests.swift`

**Interfaces:**
- Consumes: `Config.modemBaseURL`, `ModemData.parse`.
- Produces:
  - `protocol HTTPFetching: Sendable { func data(from url: URL) async throws -> Data }`
  - `struct ModemClient: Sendable { init(baseURL: URL, http: HTTPFetching); func fetch() async throws -> ModemData }`
  - `static let ModemClient.commandFields: [String]` — lista pól `cmd`.
  - `extension URLSession: HTTPFetching`.

- [ ] **Step 1: Napisz failing testy**

`Tests/ZteMenuTests/ModemClientTests.swift`:

```swift
import XCTest
@testable import ZteMenu

private struct StubHTTP: HTTPFetching {
    let payload: Data
    var capturedURL: URLBox = URLBox()
    final class URLBox: @unchecked Sendable { var url: URL? }
    func data(from url: URL) async throws -> Data {
        capturedURL.url = url
        return payload
    }
}

final class ModemClientTests: XCTestCase {
    func testBuildsQueryAndParses() async throws {
        let json = #"{"network_type":"ENDC","signalbar":"5","battery_value":"60","battery_charging":"0","ppp_status":"ppp_connected","network_provider":"T-Mobile.pl","Z5g_rsrp":"-81","Z5g_SINR":"33.0"}"#
        let stub = StubHTTP(payload: Data(json.utf8))
        let client = ModemClient(baseURL: Config.modemBaseURL, http: stub)

        let data = try await client.fetch()

        XCTAssertEqual(data.batteryPercent, 60)
        XCTAssertEqual(data.networkLabel, "5G")

        let url = try XCTUnwrap(stub.capturedURL.url)
        XCTAssertTrue(url.absoluteString.hasPrefix("http://192.168.0.1/goform/goform_get_cmd_process"))
        XCTAssertTrue(url.absoluteString.contains("multi_data=1"))
        XCTAssertTrue(url.absoluteString.contains("battery_value"))
        XCTAssertTrue(url.absoluteString.contains("Z5g_rsrp"))
    }

    func testInvalidJSONThrows() async {
        let stub = StubHTTP(payload: Data("not json".utf8))
        let client = ModemClient(baseURL: Config.modemBaseURL, http: stub)
        do {
            _ = try await client.fetch()
            XCTFail("Oczekiwano błędu")
        } catch {
            // ok
        }
    }
}
```

- [ ] **Step 2: Uruchom testy — mają nie przejść**

Run: `swift test --filter ModemClientTests`
Expected: FAIL — brak `ModemClient` / `HTTPFetching`.

- [ ] **Step 3: Zaimplementuj `ModemClient`**

`Sources/ZteMenu/ModemClient.swift`:

```swift
import Foundation

protocol HTTPFetching: Sendable {
    func data(from url: URL) async throws -> Data
}

extension URLSession: HTTPFetching {
    func data(from url: URL) async throws -> Data {
        let (data, _) = try await self.data(from: url, delegate: nil)
        return data
    }
}

struct ModemClient: Sendable {
    let baseURL: URL
    let http: HTTPFetching

    static let commandFields = [
        "network_type", "signalbar",
        "battery_value", "battery_charging",
        "ppp_status", "network_provider",
        "Z5g_rsrp", "Z5g_SINR",
    ]

    init(baseURL: URL = Config.modemBaseURL, http: HTTPFetching = URLSession.shared) {
        self.baseURL = baseURL
        self.http = http
    }

    func fetch() async throws -> ModemData {
        var comps = URLComponents(url: baseURL.appendingPathComponent("goform/goform_get_cmd_process"),
                                  resolvingAgainstBaseURL: false)!
        comps.queryItems = [
            URLQueryItem(name: "isTest", value: "false"),
            URLQueryItem(name: "cmd", value: Self.commandFields.joined(separator: ",")),
            URLQueryItem(name: "multi_data", value: "1"),
        ]
        let data = try await http.data(from: comps.url!)
        let raw = try JSONDecoder().decode([String: String].self, from: data)
        return ModemData.parse(raw)
    }
}
```

Uwaga: modem zwraca wszystkie wartości jako stringi, więc `[String: String]` dekoduje się poprawnie.

- [ ] **Step 4: Uruchom testy — mają przejść**

Run: `swift test --filter ModemClientTests`
Expected: PASS (2 testy).

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: klient HTTP modemu (ModemClient, bez logowania)"
```

---

### Task 5: Monitor WiFi (`WiFiMonitor`)

Sprawdza aktualne SSID przez CoreWLAN i zdaje, czy jesteśmy w sieci docelowej. Odczyt SSID na macOS 14+ wymaga uprawnień lokalizacji — bramka przez CoreLocation. Logika porównania SSID jest czysta i testowana; odczyt systemowy jest cienki.

**Files:**
- Create: `Sources/ZteMenu/WiFiMonitor.swift`
- Test: `Tests/ZteMenuTests/WiFiMonitorTests.swift`

**Interfaces:**
- Consumes: `Config.targetSSID`.
- Produces:
  - `protocol SSIDReading: Sendable { func currentSSID() -> String? }`
  - `struct WiFiMonitor: Sendable { init(targetSSID: String, reader: SSIDReading); var isOnTargetNetwork: Bool { get } }`
  - `struct CoreWLANReader: SSIDReading` (realny odczyt; nietestowany jednostkowo).

- [ ] **Step 1: Napisz failing testy**

`Tests/ZteMenuTests/WiFiMonitorTests.swift`:

```swift
import XCTest
@testable import ZteMenu

private struct FixedSSID: SSIDReading {
    let value: String?
    func currentSSID() -> String? { value }
}

final class WiFiMonitorTests: XCTestCase {
    func testOnTargetNetwork() {
        let m = WiFiMonitor(targetSSID: "ZTE_B4B622", reader: FixedSSID(value: "ZTE_B4B622"))
        XCTAssertTrue(m.isOnTargetNetwork)
    }

    func testDifferentNetwork() {
        let m = WiFiMonitor(targetSSID: "ZTE_B4B622", reader: FixedSSID(value: "Dom_5G"))
        XCTAssertFalse(m.isOnTargetNetwork)
    }

    func testNoWiFi() {
        let m = WiFiMonitor(targetSSID: "ZTE_B4B622", reader: FixedSSID(value: nil))
        XCTAssertFalse(m.isOnTargetNetwork)
    }
}
```

- [ ] **Step 2: Uruchom testy — mają nie przejść**

Run: `swift test --filter WiFiMonitorTests`
Expected: FAIL — brak `WiFiMonitor`.

- [ ] **Step 3: Zaimplementuj `WiFiMonitor` + `CoreWLANReader`**

`Sources/ZteMenu/WiFiMonitor.swift`:

```swift
import Foundation
import CoreWLAN

protocol SSIDReading: Sendable {
    func currentSSID() -> String?
}

struct WiFiMonitor: Sendable {
    let targetSSID: String
    let reader: SSIDReading

    init(targetSSID: String = Config.targetSSID, reader: SSIDReading = CoreWLANReader()) {
        self.targetSSID = targetSSID
        self.reader = reader
    }

    var isOnTargetNetwork: Bool {
        reader.currentSSID() == targetSSID
    }
}

struct CoreWLANReader: SSIDReading {
    func currentSSID() -> String? {
        CWWiFiClient.shared().interface()?.ssid()
    }
}
```

- [ ] **Step 4: Uruchom testy — mają przejść**

Run: `swift test --filter WiFiMonitorTests`
Expected: PASS (3 testy).

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: monitor WiFi (WiFiMonitor + CoreWLANReader)"
```

---

### Task 6: Bramka uprawnień lokalizacji (`LocationPermission`)

Odczyt SSID zwraca `nil` bez zgody na lokalizację. Ta jednostka prosi o zgodę i raportuje status, by menu mogło pokazać komunikat. Cienka otoczka nad `CLLocationManager`; testujemy tylko mapowanie statusu na typ wyliczeniowy.

**Files:**
- Create: `Sources/ZteMenu/LocationPermission.swift`
- Test: `Tests/ZteMenuTests/LocationPermissionTests.swift`

**Interfaces:**
- Consumes: nic.
- Produces:
  - `enum LocationAuth: Equatable { case notDetermined, denied, authorized }`
  - `static func LocationAuth.from(_ status: CLAuthorizationStatus) -> LocationAuth`
  - `@MainActor final class LocationPermission: NSObject` z `func requestIfNeeded()` i `var status: LocationAuth`.

- [ ] **Step 1: Napisz failing test (mapowanie statusu)**

`Tests/ZteMenuTests/LocationPermissionTests.swift`:

```swift
import XCTest
import CoreLocation
@testable import ZteMenu

final class LocationPermissionTests: XCTestCase {
    func testMapsStatuses() {
        XCTAssertEqual(LocationAuth.from(.notDetermined), .notDetermined)
        XCTAssertEqual(LocationAuth.from(.denied), .denied)
        XCTAssertEqual(LocationAuth.from(.restricted), .denied)
        XCTAssertEqual(LocationAuth.from(.authorized), .authorized)
        XCTAssertEqual(LocationAuth.from(.authorizedAlways), .authorized)
    }
}
```

- [ ] **Step 2: Uruchom test — ma nie przejść**

Run: `swift test --filter LocationPermissionTests`
Expected: FAIL — brak `LocationAuth`.

- [ ] **Step 3: Zaimplementuj `LocationAuth` + `LocationPermission`**

`Sources/ZteMenu/LocationPermission.swift`:

```swift
import Foundation
import CoreLocation

enum LocationAuth: Equatable {
    case notDetermined, denied, authorized

    static func from(_ status: CLAuthorizationStatus) -> LocationAuth {
        switch status {
        case .notDetermined: return .notDetermined
        case .denied, .restricted: return .denied
        case .authorized, .authorizedAlways: return .authorized
        @unknown default: return .denied
        }
    }
}

@MainActor
final class LocationPermission: NSObject, CLLocationManagerDelegate {
    private let manager = CLLocationManager()
    private(set) var status: LocationAuth = .notDetermined
    var onChange: (@MainActor (LocationAuth) -> Void)?

    override init() {
        super.init()
        manager.delegate = self
        status = LocationAuth.from(manager.authorizationStatus)
    }

    func requestIfNeeded() {
        if manager.authorizationStatus == .notDetermined {
            manager.requestWhenInUseAuthorization()
        }
    }

    nonisolated func locationManagerDidChangeAuthorization(_ manager: CLLocationManager) {
        let mapped = LocationAuth.from(manager.authorizationStatus)
        Task { @MainActor in
            self.status = mapped
            self.onChange?(mapped)
        }
    }
}
```

- [ ] **Step 4: Uruchom test — ma przejść**

Run: `swift test --filter LocationPermissionTests`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: bramka uprawnień lokalizacji (LocationPermission)"
```

---

### Task 7: Stan aplikacji (`ModemStore`)

Obserwowalny stan spinający WiFi + klienta. Wyznacza `AppState` (`hidden` / `connected` / `error`) i steruje odświeżaniem. Logika przejść stanu jest czysta i testowana; timer dołączany osobno.

**Files:**
- Create: `Sources/ZteMenu/AppState.swift`
- Create: `Sources/ZteMenu/ModemStore.swift`
- Test: `Tests/ZteMenuTests/ModemStoreTests.swift`

**Interfaces:**
- Consumes: `WiFiMonitor`, `ModemClient`, `LocationAuth`, `ModemData`.
- Produces:
  - `enum AppState: Equatable { case hidden; case connected(ModemData); case locationDenied; case error(String) }`
  - `@MainActor @Observable final class ModemStore` z `var state: AppState`, `init(monitor:client:)`, `func refresh() async`, `func setLocationAuth(_:)`.

- [ ] **Step 1: Napisz failing testy**

`Tests/ZteMenuTests/ModemStoreTests.swift`:

```swift
import XCTest
@testable import ZteMenu

private struct FixedSSID: SSIDReading {
    let value: String?
    func currentSSID() -> String? { value }
}
private struct StubHTTP: HTTPFetching {
    let payload: Data
    let shouldThrow: Bool
    func data(from url: URL) async throws -> Data {
        if shouldThrow { throw URLError(.cannotConnectToHost) }
        return payload
    }
}

@MainActor
final class ModemStoreTests: XCTestCase {
    private let goodJSON = Data(#"{"signalbar":"5","battery_value":"60","battery_charging":"0","network_type":"ENDC","ppp_status":"ppp_connected"}"#.utf8)

    private func makeStore(ssid: String?, throwing: Bool = false) -> ModemStore {
        let monitor = WiFiMonitor(targetSSID: "ZTE_B4B622", reader: FixedSSID(value: ssid))
        let client = ModemClient(baseURL: Config.modemBaseURL,
                                 http: StubHTTP(payload: goodJSON, shouldThrow: throwing))
        return ModemStore(monitor: monitor, client: client)
    }

    func testHiddenWhenOffNetwork() async {
        let store = makeStore(ssid: "Inne")
        await store.refresh()
        XCTAssertEqual(store.state, .hidden)
    }

    func testConnectedWhenOnNetwork() async {
        let store = makeStore(ssid: "ZTE_B4B622")
        await store.refresh()
        if case .connected(let d) = store.state {
            XCTAssertEqual(d.batteryPercent, 60)
        } else {
            XCTFail("Oczekiwano connected, było \(store.state)")
        }
    }

    func testErrorWhenModemUnreachable() async {
        let store = makeStore(ssid: "ZTE_B4B622", throwing: true)
        await store.refresh()
        if case .error = store.state {} else {
            XCTFail("Oczekiwano error, było \(store.state)")
        }
    }

    func testLocationDeniedOverridesToDeniedWhenOnNetwork() async {
        let store = makeStore(ssid: "ZTE_B4B622")
        store.setLocationAuth(.denied)
        await store.refresh()
        // Bez zgody nie da się potwierdzić SSID — traktujemy jak locationDenied.
        XCTAssertEqual(store.state, .locationDenied)
    }
}
```

- [ ] **Step 2: Uruchom testy — mają nie przejść**

Run: `swift test --filter ModemStoreTests`
Expected: FAIL — brak `AppState` / `ModemStore`.

- [ ] **Step 3: Zaimplementuj `AppState`**

`Sources/ZteMenu/AppState.swift`:

```swift
import Foundation

enum AppState: Equatable {
    case hidden
    case connected(ModemData)
    case locationDenied
    case error(String)
}
```

- [ ] **Step 4: Zaimplementuj `ModemStore`**

`Sources/ZteMenu/ModemStore.swift`:

```swift
import Foundation
import Observation

@MainActor
@Observable
final class ModemStore {
    private(set) var state: AppState = .hidden
    private let monitor: WiFiMonitor
    private let client: ModemClient
    private var locationAuth: LocationAuth = .authorized

    init(monitor: WiFiMonitor = WiFiMonitor(), client: ModemClient = ModemClient()) {
        self.monitor = monitor
        self.client = client
    }

    func setLocationAuth(_ auth: LocationAuth) {
        locationAuth = auth
    }

    func refresh() async {
        // Bez zgody na lokalizację odczyt SSID jest niewiarygodny.
        if locationAuth == .denied {
            state = .locationDenied
            return
        }
        guard monitor.isOnTargetNetwork else {
            state = .hidden
            return
        }
        do {
            let data = try await client.fetch()
            state = .connected(data)
        } catch {
            state = .error("Nie można połączyć z modemem")
        }
    }
}
```

- [ ] **Step 5: Uruchom testy — mają przejść**

Run: `swift test --filter ModemStoreTests`
Expected: PASS (4 testy).

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: stan aplikacji (AppState + ModemStore)"
```

---

### Task 8: Prezentacja ikony (`MenuBarPresentation`)

Czysta logika mapująca `AppState` na nazwę SF Symbol, `variableValue` i widoczność ikony. Wydzielona, by przetestować wygląd bez SwiftUI.

**Files:**
- Create: `Sources/ZteMenu/MenuBarPresentation.swift`
- Test: `Tests/ZteMenuTests/MenuBarPresentationTests.swift`

**Interfaces:**
- Consumes: `AppState`, `ModemData`.
- Produces:
  - `struct MenuBarPresentation: Equatable { let isVisible: Bool; let symbolName: String; let variableValue: Double }`
  - `static func MenuBarPresentation.make(for state: AppState) -> MenuBarPresentation`

- [ ] **Step 1: Napisz failing testy**

`Tests/ZteMenuTests/MenuBarPresentationTests.swift`:

```swift
import XCTest
@testable import ZteMenu

final class MenuBarPresentationTests: XCTestCase {
    private func data(bars: Int) -> ModemData {
        ModemData.parse(["signalbar": "\(bars)", "network_type": "ENDC", "ppp_status": "ppp_connected"])
    }

    func testHiddenIsInvisible() {
        XCTAssertFalse(MenuBarPresentation.make(for: .hidden).isVisible)
    }

    func testConnectedUsesCellularbars() {
        let p = MenuBarPresentation.make(for: .connected(data(bars: 4)))
        XCTAssertTrue(p.isVisible)
        XCTAssertEqual(p.symbolName, "cellularbars")
        XCTAssertEqual(p.variableValue, 0.8, accuracy: 0.001) // 4/5
    }

    func testNoSignalUsesSlashSymbol() {
        let p = MenuBarPresentation.make(for: .connected(data(bars: 0)))
        XCTAssertEqual(p.symbolName, "antenna.radiowaves.left.and.right.slash")
    }

    func testErrorIsVisibleWithSlash() {
        let p = MenuBarPresentation.make(for: .error("x"))
        XCTAssertTrue(p.isVisible)
        XCTAssertEqual(p.symbolName, "antenna.radiowaves.left.and.right.slash")
    }

    func testLocationDeniedIsVisible() {
        XCTAssertTrue(MenuBarPresentation.make(for: .locationDenied).isVisible)
    }
}
```

- [ ] **Step 2: Uruchom testy — mają nie przejść**

Run: `swift test --filter MenuBarPresentationTests`
Expected: FAIL — brak `MenuBarPresentation`.

- [ ] **Step 3: Zaimplementuj `MenuBarPresentation`**

`Sources/ZteMenu/MenuBarPresentation.swift`:

```swift
import Foundation

struct MenuBarPresentation: Equatable {
    let isVisible: Bool
    let symbolName: String
    let variableValue: Double

    static func make(for state: AppState) -> MenuBarPresentation {
        switch state {
        case .hidden:
            return MenuBarPresentation(isVisible: false, symbolName: "cellularbars", variableValue: 0)
        case .connected(let d):
            if d.signalBars <= 0 {
                return MenuBarPresentation(isVisible: true,
                                           symbolName: "antenna.radiowaves.left.and.right.slash",
                                           variableValue: 0)
            }
            return MenuBarPresentation(isVisible: true,
                                       symbolName: "cellularbars",
                                       variableValue: Double(min(d.signalBars, 5)) / 5.0)
        case .locationDenied, .error:
            return MenuBarPresentation(isVisible: true,
                                       symbolName: "antenna.radiowaves.left.and.right.slash",
                                       variableValue: 0)
        }
    }
}
```

- [ ] **Step 4: Uruchom testy — mają przejść**

Run: `swift test --filter MenuBarPresentationTests`
Expected: PASS (5 testów).

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: prezentacja ikony paska (MenuBarPresentation)"
```

---

### Task 9: Widok SwiftUI i punkt wejścia (`MenuBarView` + `App`)

Łączy wszystko w działającą aplikację `MenuBarExtra`. Renderuje ikonę wg `MenuBarPresentation`, rozwijane menu wg `AppState`, uruchamia pętlę odświeżania co 60 s i prosi o lokalizację przy starcie. Warstwa widoku — bez testów jednostkowych (weryfikacja ręczna w Task 11).

**Files:**
- Create: `Sources/ZteMenu/MenuBarView.swift`
- Create: `Sources/ZteMenu/ZteMenuApp.swift`
- Create: `Sources/zte-menu/main.swift` (cienki punkt wejścia executable'a)
- Modify: `Package.swift` (dodaj executable target `zte-menu`)
- Modify: pliki z Tasków 2,4,5,7 — poszerzenie widoczności do `public` (patrz Step 0)
- Delete: `Sources/ZteMenu/Placeholder.swift`

**Interfaces:**
- Consumes: `ModemStore`, `AppState`, `ModemData`, `MenuBarPresentation`, `LocationPermission`, `Config`.
- Produces: executable `zte-menu`.

**Struktura punktu wejścia (WAŻNE):** `@main` NIE może żyć w bibliotece
`ZteMenu` linkowanej do osobnego `executableTarget` — powstają dwa symbole
`_main` i linker pada z „duplicate symbol '_main'". Dlatego:
`ZteMenuApp` (typ `App`) mieszka w bibliotece BEZ `@main`, a executable
`zte-menu` ma własny `Sources/zte-menu/main.swift` z kodem najwyższego poziomu
`ZteMenuApp.main()`. `SwiftUI.App` udostępnia statyczne `main()` bez wymogu
`@main`, więc to działa i nie tworzy drugiego `_main`.

- [ ] **Step 0: Poszerz widoczność typów do `public`**

`MenuBarView.init` jest `public` i w publicznej sygnaturze odwołuje się do
typów z wcześniejszych tasków, które były `internal`. Poszerz do `public`
TYLKO to, co jest osiągalne z publicznej sygnatury (nie więcej):

- `Sources/ZteMenu/Config.swift`: `enum Config`, `modemBaseURL`, `targetSSID` → `public`. `refreshInterval` może zostać `internal` (używane tylko w ciele metody).
- `Sources/ZteMenu/ModemClient.swift`: `struct ModemClient` + `init`, `protocol HTTPFetching`, metoda konformancji `URLSession.data(from:)` → `public`.
- `Sources/ZteMenu/WiFiMonitor.swift`: `struct WiFiMonitor` + `init`, `protocol SSIDReading`, `struct CoreWLANReader` + `init()` + `currentSSID()` → `public`.
- `Sources/ZteMenu/ModemStore.swift`: `class ModemStore` + `init` → `public`. `state`, `setLocationAuth`, `refresh` mogą zostać `internal` (dotykane tylko z ciała metod w tym samym module).

Zasada: poszerzaj minimalnie, error-by-error z kompilatora. Nie oznaczaj
`public` niczego, czego nie wymaga publiczna sygnatura.

- [ ] **Step 1: Zaktualizuj `Package.swift` o executable + resources**

Zamień zawartość `Package.swift`:

```swift
// swift-tools-version: 6.0
import PackageDescription

let package = Package(
    name: "ZteMenu",
    platforms: [.macOS(.v14)],
    products: [
        .executable(name: "zte-menu", targets: ["zte-menu"]),
    ],
    targets: [
        .target(name: "ZteMenu"),
        .executableTarget(name: "zte-menu", dependencies: ["ZteMenu"]),
        .testTarget(name: "ZteMenuTests", dependencies: ["ZteMenu"]),
    ]
)
```

Executable target `zte-menu` wymaga własnego katalogu źródeł
`Sources/zte-menu/` (patrz Step 4a) — sam „import" biblioteki nie wystarcza.

- [ ] **Step 2: Usuń placeholder**

```bash
git rm Sources/ZteMenu/Placeholder.swift
```

(Smoke test w `SmokeTest.swift` odwołuje się do `ZteMenuBuildMarker` — usuń też ten plik testowy.)

```bash
git rm Tests/ZteMenuTests/SmokeTest.swift
```

- [ ] **Step 3: Napisz `MenuBarView.swift`**

`Sources/ZteMenu/MenuBarView.swift`:

```swift
import SwiftUI

public struct MenuBarView: View {
    @State private var store: ModemStore

    public init(store: ModemStore = ModemStore()) {
        _store = State(initialValue: store)
    }

    public var body: some View {
        content
    }

    @ViewBuilder
    private var content: some View {
        switch store.state {
        case .hidden:
            Text("Brak połączenia z ZTE_B4B622")
            Divider()
            Button("Zakończ") { NSApplication.shared.terminate(nil) }
        case .locationDenied:
            Text("⚠️ Włącz uprawnienia lokalizacji, aby wykrywać sieć WiFi")
            Divider()
            Button("Zakończ") { NSApplication.shared.terminate(nil) }
        case .error(let msg):
            Text("⚠️ \(msg)")
            Divider()
            Button("Odśwież teraz") { Task { await store.refresh() } }
            Button("Zakończ") { NSApplication.shared.terminate(nil) }
        case .connected(let d):
            connectedMenu(d)
        }
    }

    @ViewBuilder
    private func connectedMenu(_ d: ModemData) -> some View {
        Text("ZTE U50 · \(Config.targetSSID)")
        Divider()
        if let b = d.batteryPercent {
            Text("🔋 Bateria: \(b)%\(d.isCharging ? " (ładowanie)" : "")")
        }
        Text("📶 Sygnał: \(d.signalDescription) (\(d.signalBars)/5)")
        Text("📡 Sieć: \(d.networkLabel)")
        if let r = d.rsrp { Text("📊 RSRP: \(r) dBm") }
        if let s = d.sinr { Text("📊 SINR: \(String(format: "%.0f", s)) dB") }
        if let p = d.provider { Text("🏢 Operator: \(p)") }
        Divider()
        Button("Odśwież teraz") { Task { await store.refresh() } }
        Button("Zakończ") { NSApplication.shared.terminate(nil) }
    }
}
```

- [ ] **Step 4: Napisz `ZteMenuApp.swift` (typ `App`, BEZ `@main`)**

`Sources/ZteMenu/ZteMenuApp.swift`:

```swift
import SwiftUI

public struct ZteMenuApp: App {
    @State private var store = ModemStore()

    public init() {}

    public var body: some Scene {
        let p = MenuBarPresentation.make(for: store.state)
        return MenuBarExtra(isInserted: .constant(p.isVisible)) {
            MenuBarView(store: store)
        } label: {
            Image(systemName: p.symbolName, variableValue: p.variableValue)
        }
        .menuBarExtraStyle(.menu)
    }
}
```

Uwaga: BRAK `@main` tutaj — patrz sekcja „Struktura punktu wejścia". Punkt
wejścia jest w Step 4a. Pętla odświeżania i żądanie uprawnień wymagają miejsca
na `.task`; `App`/`Scene` go nie ma, więc żyją w `MenuBarView` (Step 5) — widok
menu tworzony jest raz przy starcie i utrzymywany. `store` jest jednym źródłem
prawdy współdzielonym między etykietą ikony a widokiem menu; jego zmiany
(`@Observable`) przerysowują etykietę, więc ikona chowa się/pokazuje wraz ze stanem.

- [ ] **Step 4a: Napisz `main.swift` executable'a**

`Sources/zte-menu/main.swift` (plik MUSI nazywać się `main.swift`, by kod
najwyższego poziomu był punktem wejścia; `ZteMenuApp.main()` uruchamia App
bez `@main`):

```swift
import ZteMenu

ZteMenuApp.main()
```

- [ ] **Step 5: Dodaj pętlę odświeżania i żądanie uprawnień w `MenuBarView`**

Zmodyfikuj plik `Sources/ZteMenu/MenuBarView.swift` z Step 3. Najpierw dodaj właściwość tuż pod istniejącym `@State private var store`:

```swift
    @State private var permission = LocationPermission()
```

Następnie zmień `body`, owijając `content` w `.task` (zastępując dotychczasowe `public var body: some View { content }`):

```swift
    public var body: some View {
        content
            .task {
                permission.onChange = { auth in store.setLocationAuth(auth) }
                store.setLocationAuth(permission.status)
                permission.requestIfNeeded()
                await store.refresh()
                while !Task.isCancelled {
                    try? await Task.sleep(for: .seconds(Config.refreshInterval))
                    await store.refresh()
                }
            }
    }
```

- [ ] **Step 6: Zbuduj — ma się skompilować**

Run: `swift build`
Expected: build OK (mogą być ostrzeżenia; brak błędów).

- [ ] **Step 7: Uruchom pełne testy — mają przejść**

Run: `swift test`
Expected: PASS (wszystkie wcześniejsze testy; smoke test usunięty).

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "feat: widok MenuBarExtra i punkt wejścia aplikacji"
```

---

### Task 10: Skrypt pakujący do `.app` (LSUIElement)

Buduje release i składa bundel `.app` z `Info.plist` ustawiającym LSUIElement (brak Docka) i `NSLocationWhenInUseUsageDescription` (opis prośby o lokalizację). Zgodnie ze skillem `macos-spm-app-packaging`.

**Files:**
- Create: `Resources/Info.plist`
- Create: `scripts/build-app.sh`
- Test: uruchomienie skryptu (ręczne).

**Interfaces:**
- Consumes: executable `zte-menu` z `swift build -c release`.
- Produces: `dist/ZTE Menu.app`.

- [ ] **Step 1: Napisz `Resources/Info.plist`**

`Resources/Info.plist`:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key>
    <string>ZTE Menu</string>
    <key>CFBundleDisplayName</key>
    <string>ZTE Menu</string>
    <key>CFBundleIdentifier</key>
    <string>io.8lines.zte-menu</string>
    <key>CFBundleVersion</key>
    <string>1.0</string>
    <key>CFBundleShortVersionString</key>
    <string>1.0</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleExecutable</key>
    <string>zte-menu</string>
    <key>LSMinimumSystemVersion</key>
    <string>14.0</string>
    <key>LSUIElement</key>
    <true/>
    <key>NSLocationWhenInUseUsageDescription</key>
    <string>Aplikacja odczytuje nazwę sieci WiFi, aby pokazać status modemu tylko gdy jesteś podłączony do ZTE_B4B622.</string>
</dict>
</plist>
```

- [ ] **Step 2: Napisz `scripts/build-app.sh`**

`scripts/build-app.sh`:

```bash
#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."

APP_NAME="ZTE Menu"
BUNDLE_ID="io.8lines.zte-menu"
EXECUTABLE="zte-menu"
DIST="dist"
APP="$DIST/$APP_NAME.app"

echo "==> swift build -c release"
swift build -c release

BIN_PATH="$(swift build -c release --show-bin-path)/$EXECUTABLE"
if [[ ! -f "$BIN_PATH" ]]; then
  echo "Nie znaleziono binarki: $BIN_PATH" >&2
  exit 1
fi

echo "==> Składanie bundla $APP"
rm -rf "$APP"
mkdir -p "$APP/Contents/MacOS"
mkdir -p "$APP/Contents/Resources"
cp "$BIN_PATH" "$APP/Contents/MacOS/$EXECUTABLE"
cp "Resources/Info.plist" "$APP/Contents/Info.plist"

echo "==> Ad-hoc signing"
codesign --force --deep --sign - "$APP" || echo "Uwaga: codesign nieudany (można uruchomić i tak lokalnie)"

echo "==> Gotowe: $APP"
```

- [ ] **Step 3: Nadaj prawa wykonywania i uruchom skrypt**

Run:
```bash
chmod +x scripts/build-app.sh && ./scripts/build-app.sh
```
Expected: build release OK, powstaje `dist/ZTE Menu.app`, komunikat „Gotowe".

- [ ] **Step 4: Zweryfikuj strukturę bundla**

Run: `ls -R "dist/ZTE Menu.app" && plutil -lint "dist/ZTE Menu.app/Contents/Info.plist"`
Expected: obecne `Contents/MacOS/zte-menu` i `Contents/Info.plist`; `Info.plist` OK.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "build: skrypt pakujący do .app (LSUIElement + uprawnienia lokalizacji)"
```

---

### Task 11: Weryfikacja end-to-end na żywo

Uruchomienie prawdziwej aplikacji na Macu podłączonym do `ZTE_B4B622` i potwierdzenie, że ikona oraz menu działają. Wymaga interakcji użytkownika (kliknięcie ikony, ewentualna zgoda na lokalizację).

**Files:** brak zmian kodu (chyba że wyjdą błędy — wtedy poprawki + commit).

- [ ] **Step 1: Uruchom aplikację**

Run: `open "dist/ZTE Menu.app"`
Expected: w pasku menu pojawia się ikona `cellularbars` (jesteś w sieci ZTE_B4B622). Przy pierwszym starcie macOS może zapytać o zgodę na lokalizację — zaakceptuj.

- [ ] **Step 2: Zweryfikuj menu**

Kliknij ikonę. Expected: menu pokazuje „ZTE U50 · ZTE_B4B622", baterię w %, sygnał (opis + x/5), sieć „5G", RSRP, SINR, operator „T-Mobile.pl", oraz „Odśwież teraz" / „Zakończ".

- [ ] **Step 3: Zweryfikuj znikanie ikony poza siecią (jeśli możliwe)**

Rozłącz WiFi lub przełącz na inną sieć, odczekaj do 60 s (lub kliknij „Odśwież teraz" tuż przed). Expected: ikona znika z paska. Po powrocie do ZTE_B4B622 — wraca.

- [ ] **Step 4: Zweryfikuj brak ikony w Docku**

Expected: aplikacja nie ma ikony w Docku (LSUIElement działa).

- [ ] **Step 5: Zamknij przez menu**

Kliknij „Zakończ". Expected: aplikacja się kończy, ikona znika.

- [ ] **Step 6 (jeśli były poprawki): Commit**

```bash
git add -A
git commit -m "fix: poprawki po weryfikacji end-to-end"
```

---

## Uwagi wykonawcze

- **CoreLocation i wątki:** delegat `locationManagerDidChangeAuthorization` jest `nonisolated`; skacze na `@MainActor` przez `Task`. To poprawne w Swift 6.
- **Odczyt SSID zwraca `nil` bez zgody:** dlatego `ModemStore.refresh()` traktuje `.denied` jako `locationDenied` zanim spróbuje odczytać SSID.
- **`MenuBarExtra` + dynamiczna widoczność:** `isInserted:` sterowane przez `MenuBarPresentation.isVisible`. Gdy `false`, ikona znika z paska — to realizuje wymóg „ikona całkowicie znika".
- **Gdyby ikona nie chowała się dynamicznie** (znane ograniczenia `MenuBarExtra` w niektórych wersjach): fallback to sterowanie `NSStatusItem` bezpośrednio przez `AppDelegate`. Zanotować i zdecydować w Task 11, jeśli Step 3 zawiedzie — użyć wtedy skilla `systematic-debugging`.
- **Bezpieczeństwo/prywatność:** aplikacja działa tylko w LAN, odpytuje lokalny modem, nie wysyła danych na zewnątrz.
